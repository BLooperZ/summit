import React, {Component} from 'react'
const Buttons = () => (
<div className ="btns">
<button className="btn" onClick={()=>alert("blalsd")}>Save
    </button>
    <button className="btn" onClick={()=>alert("blalsd")}>Post
    </button>
    <button className="btn" onClick={()=>alert("blalsd")}>Q&A
    </button>
    </div>
)
export default Buttons